#include <stdio.h>

/**
 * main - where the function starts
 * str: str c
 * Return: returns zero
 */
int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);
	return (0);
}
